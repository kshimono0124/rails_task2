<div class="container-fiuld room">
	<h4 class="pb-4">検索結果：
	  <% if @results == nil %>
      0件
    <% else %>
      <%= @results.count %>件
    <% end %>
	</h4>
	<div class="row">
    <% @results.each do |room| %>
		<div class="col-sm-6 col-lg-4 col-xl-3">
			<div class="room-frame-inner">
					<div class="room-image-out">
					  <%= link_to image_tag("#{room.image}", onerror: "this.onerror=null;this.src='/assets/room01.jpg';", alt: "部屋画像", class: "room-image" ),
					  room_path(room) %>
					</div>
					<div class="room-profile">
						<p class="room-profile-name"><%= room.name %></p>
						<p class="room-profile-city"><%= room.content %></p>
						<p class="room-profile-price price-icon"><%= room.price %></p>
					</div>
				</a>
			</div>
		</div>
		<% end %>
	</div>
</div>